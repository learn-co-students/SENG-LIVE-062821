"use strict";

var _require = require("./get-app-info"),
    getAppInfo = _require.getAppInfo;

function runner() {
  var _getAppInfo = getAppInfo(),
      fileInfo = _getAppInfo.fileInfo,
      imports = _getAppInfo.imports;

  return "\nimport { makeApp } from \"@ihollander/workshop-app\";\nimport pkg from '../package.json'\n\nconst fileInfo = ".concat(JSON.stringify(fileInfo, null, 2), ";\n\nif (module.hot) module.hot.accept()\n\nmakeApp({\n  imports: {\n    ").concat(imports.join(",\n      "), "\n  },\n  fileInfo,\n  projectTitle: pkg.title\n});\n  ");
}

module.exports = {
  runner: runner
};