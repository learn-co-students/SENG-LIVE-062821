import { Mask } from '../../setupWorker/glossary';
/**
 * Converts a given request handler mask into a URL, if given a valid URL string.
 */
export declare function getUrlByMask(mask: Mask): URL | Mask;
